import { inject, injectable } from "inversify";
import { Tokens } from "../../constants/Tokens.js";

@injectable
export class AuthController{
    constructor(
        // @inject(Tokens.)       use cases di
    )
}